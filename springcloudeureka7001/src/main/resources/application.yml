server:
  port: 7001
eureka:
  instance:
    hostname: eureka7001
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001:7003/eureka/,http://eureka7002:7002/eureka/

#defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/